---
interface Props {
  date: Date;
  updated?: boolean;
}

const { date, updated } = Astro.props;
---

<time class="text-sm text-gray-500" datetime={date.toISOString()}>
  {updated ? 'Last updated: ' : ''}{date.toISOString().split('T')[0]}
</time>
